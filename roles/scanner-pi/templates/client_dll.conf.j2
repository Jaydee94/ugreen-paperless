# /etc/sane.d/dll.conf
# Enabled backends for system clients (scanimage)
# Only 'net' should be enabled to force connection via scanbd.
# Direct access drivers (fujitsu) must be disabled to avoid resource conflicts.

net
